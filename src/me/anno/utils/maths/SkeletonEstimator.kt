package me.anno.utils.maths

import me.anno.ecs.components.mesh.Mesh
import org.joml.Vector4f

object SkeletonEstimator {

    /*fun estimateSkeleton(mesh: Mesh) {


        // to do find all bones

        // to do find the position and direction of the bones using the normals

        val boneWeights = mesh.boneWeights!!
        val boneIndices = mesh.boneIndices!!

        val positions = mesh.positions!!

        val boneCount = boneIndices.maxOrNull()!!.toInt().and(255)

        val estimatedNormals = Array(boneCount) { Vector4f() }
        val indices = mesh.indices ?: IntArray(positions.size / 3) { it }

        for (index in indices) {

        }


    }*/

}