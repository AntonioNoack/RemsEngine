package me.anno.tests.export

import org.junit.jupiter.api.Test

class SimpleExportTest {
    @Test
    fun createEngineBuild() {
        // todo create an engine build,
        //  define a script/component, that creates a file?
        //  and then quit the engine, and check that that file exists???
    }
}